"""Self-contained factory measurement modules shipped with the product.

``app/factory/build/authorship.py`` is stdlib-only and exists here so the
product can measure its own authorship floor in a container that has no
factory host: scripts/acceptance.py imports
``app.factory.build.authorship.full_pilot_authorship_from``.
"""
