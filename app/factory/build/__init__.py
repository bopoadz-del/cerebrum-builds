"""Vendored slice of the factory build package (authorship meter).

Deliberately empty: the factory's own package __init__ imports the
whole manufacturing kernel, which a delivered platform must not ship.
docs/coder_receipt.json carries the measured authorship this module
re-derives for scripts/acceptance.py.
"""
