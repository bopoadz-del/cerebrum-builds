{
 "operator_settings": [
  {
   "name": "CURRENCY",
   "reason": "the brief named no country and no currency for pricing",
   "required": true
  },
  {
   "name": "BROKER_COMMISSION_PERCENT",
   "reason": "the brief named no broker commission",
   "required": true
  },
  {
   "name": "TAX_RATE_PERCENT",
   "reason": "no tax regime was named",
   "required": false
  },
  {
   "name": "DAILY_CALL_CAP",
   "reason": "the operator's own pacing cap (default stated, not assumed)",
   "required": false
  },
  {
   "name": "CONCURRENCY",
   "reason": "the operator's own dial concurrency (default stated, not assumed)",
   "required": false
  },
  {
   "name": "BROKER_TRANSFER_NUMBER",
   "reason": "the human broker's line for a warm transfer; no default, and with neither it nor a record broker_number the transfer is recorded as declined",
   "required": true
  },
  {
   "name": "DXB_DATA_MCP_URL",
   "reason": "optional CHADi pilot egress for official DLD area medians/yields via DXB Data MCP; unset = not_configured",
   "required": false
  },
  {
   "name": "DLD_QUERY_URL",
   "reason": "optional CHADi pilot egress for DLD sales/Ejari (dld-mcp data plane; uvx --with 'mcp<2' dld-mcp); unset = not_configured",
   "required": false
  },
  {
   "name": "APIFY_TOKEN",
   "reason": "optional CHADi pilot egress for capped Bayut for-sale harvest via memo23/apify-bayut-scraper; unset = not_configured",
   "required": false
  }
 ],
 "product_id": "real-estate",
 "product_name": "CallOps",
 "schema": "blockers.v1",
 "configured_egress": [
  {
   "name": "dxb_data",
   "settings": ["DXB_DATA_MCP_URL"],
   "routes": [
    "POST /v1/market/dxb/snapshot",
    "POST /v1/market/dxb/yield",
    "POST /v1/market/dxb/compare"
   ],
   "state": "real client when DXB_DATA_MCP_URL is set; not_configured otherwise; never fabricates"
  },
  {
   "name": "dld",
   "settings": ["DLD_QUERY_URL"],
   "mcp_command": "uvx --with 'mcp<2' dld-mcp",
   "routes": ["POST /v1/market/dld/sales_stats"],
   "state": "real client when DLD_QUERY_URL is set; not_configured otherwise; never fabricates"
  },
  {
   "name": "apify_bayut",
   "settings": ["APIFY_TOKEN"],
   "actor": "memo23/apify-bayut-scraper",
   "routes": ["POST /v1/market/apify/bayut_buy"],
   "state": "real client when APIFY_TOKEN is set; not_configured otherwise; harvests capped; never fabricates"
  }
 ],
 "stubbed_integrations": [
  {
   "behaviour": "with any of them unset the gateway originates against the recorded fake-Twilio fixtures in tests/fixtures/twilio/ and refuses to open a socket; CI never dials",
   "capabilities": [
    "voice_gateway",
    "warm_transfer"
   ],
   "name": "twilio_programmable_voice",
   "settings": [
    "TWILIO_ACCOUNT_SID",
    "TWILIO_AUTH_TOKEN",
    "TWILIO_CALLER_NUMBER",
    "BROKER_TRANSFER_NUMBER"
   ],
   "state": "stubbed until all three are set"
  },
  {
   "behaviour": "records the intent and the payload shape, registers the destination offline, and declares delivery_state 'stubbed'; nothing is pushed",
   "capabilities": [
    "crm_destination_placeholder"
   ],
   "name": "crm_destination",
   "state": "placeholder: no CRM destination was named in the brief"
  }
 ],
 "unavailable": [
  {
   "block_id": "knowledge",
   "capabilities": [
    "project_knowledge_grounding",
    "qualification_and_broker_summary"
   ],
   "mitigation": "retrieval is carried by app.retrieval (tenant corpus, precedence.v1) plus the vendored vector_search block; the handler records blocks_unavailable['knowledge'] and never reports the block as answered",
   "reason": "the vendored knowledge block imports vendor.cerebrum.core.vector_store, which this delivery's runtime slice does not ship; the block answers a named ImportError rather than a result"
  },
  {
   "block_id": "google_drive",
   "capabilities": [
    "google_drive"
   ],
   "mitigation": "drive_mode stays 'stubbed' and the capability declares the block unavailable; project sheets arrive through local_drive and the rag ingest route in the meantime",
   "reason": "no Google credentials were supplied; the block refuses with 'Not authenticated' by name"
  },
  {
   "block_id": "mcp_adapter",
   "capabilities": [
    "mcp_adapter"
   ],
   "mitigation": "the capability falls back to docs/block_contracts.json, generated from the same block metadata, and names the block unavailable",
   "reason": "the delivered registry is a lazy loader the adapter cannot enumerate ('_LazyRegistry' object has no attribute 'items')"
  }
 ]
}
