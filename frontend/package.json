{
  "name": "callops-console",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "React source of the CallOps operator console. The serving artefact is app/static/index.html, which is dependency-free; this tree is the same console in components, type-checked and bundled by the image and by CI.",
  "scripts": {
    "build": "vite build --outDir dist --emptyOutDir",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
