{
  "name": "callops-console",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "React source of the operator console. The serving artifact is app/static/index.html, which is dependency-free and needs no build step; this tree is the same console in components, built with `npm run build` when an operator wants a bundler.",
  "scripts": {
    "build": "vite build --outDir ../app/static --emptyOutDir false",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
