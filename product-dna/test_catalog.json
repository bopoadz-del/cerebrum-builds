{
  "schema": "product_dna.test_catalog.v1",
  "suites": [
    {
      "covers": "handlers import, blocks load and run offline, handle() returns a mapping",
      "file": "tests/test_smoke.py",
      "marker": "code"
    },
    {
      "covers": "every model round-trips through the store; envelope vocabulary enforced by schema",
      "file": "tests/test_models.py",
      "marker": "code"
    },
    {
      "covers": "every capability POST/GET/GET-id/PUT/DELETE answers; pilot: spec payload accepted and persisted",
      "file": "tests/test_routes.py",
      "marker": "code+pilot"
    },
    {
      "covers": "the served console's routes answer, drives two capabilities, uses app/formulas, shows authority labels, money settings truthful",
      "file": "tests/test_ui_surface.py",
      "marker": "code"
    },
    {
      "covers": "document ingest then query round-trips and is tenant scoped",
      "file": "tests/test_rag.py",
      "marker": "pilot"
    },
    {
      "covers": "alembic up/down on a populated v1, restore drill, parallel writes",
      "file": "tests/test_data_lifecycle.py",
      "marker": "code"
    },
    {
      "covers": "fail-closed /health, correlation logs, revision rollback identity",
      "file": "tests/test_deploy.py",
      "marker": "code"
    },
    {
      "covers": "ten business outcomes performed through the kernel",
      "file": "tests/test_domain_acceptance.py",
      "marker": "code+pilot"
    }
  ]
}
