[pytest]
testpaths = tests
# "pilot" marks the checks a complete platform performs; the code-phase gate
# runs everything else:
#   python -m pytest -m "not pilot" -q
markers =
    pilot: exercised on a migrated store; part of the pilot cycle, not the code-phase gate
filterwarnings =
    ignore::DeprecationWarning
