fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
SQLAlchemy==2.0.36
alembic==1.14.0
httpx==0.28.1

# Imported at module load by the vendored blocks this platform binds:
# numpy by vector_search (document_knowledge_qa) and PyYAML by
# document_engine (document_knowledge_qa). Without them the block cannot load inside the image
# and /health reports the bound block as unavailable.
numpy>=2.0,<3.0
PyYAML==6.0.3
