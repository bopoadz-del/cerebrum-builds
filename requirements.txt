# Runtime dependencies. Persistence is stdlib sqlite3 on purpose --
# the platform runs with no database server and no network (P1).
# pydantic is required by the vendored cerebrum_product_kernel contract.
# Alembic applies versioned schema at deploy against STORAGE_PATH.
# starlette is imported directly by app/observe.py. FastAPI pulls it in
# transitively, but a runtime module that imports a package must declare
# it: relying on someone else's dependency graph breaks the moment that
# graph changes, and F10 is exactly the class of defect where an import
# is satisfied by accident rather than by declaration.
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
alembic>=1.13
sqlalchemy>=2.0
starlette>=0.37
# Postgres, when the operator sets DATABASE_URL. Declared here so the
# deployment path is real rather than a scheme the platform cannot dial:
# app/db.py maps postgres:// and postgresql:// onto psycopg (v3), and
# alembic/env.py uses that same normalised URL for migrations.
psycopg[binary]>=3.1

# Vendored block dependencies (refreshed by the factory).
Pint  # action: pint in formula_executor.py
aiofiles  # module: aiofiles in storage.py
aiohttp  # action: aiohttp in webhook.py
aiosmtplib  # action: aiosmtplib in notification.py
cryptography  # action: cryptography in google_drive.py
httpx  # action: httpx in formula_executor.py, google_drive.py, knowledge.py
numpy  # module: numpy in formula_executor.py, vector_search.py
psycopg2-binary  # action: psycopg2 in database.py
redis  # action: redis in queue.py
scikit-learn  # module: sklearn in vector_search.py
sympy  # action: sympy in formula_executor.py
